# Version History

## v1.4.0
* **パフォーマンス大幅向上**: Web Worker による並列・バックグラウンド変換処理に対応し、大容量のリソースパック変換時でもUIがフリーズしないスムーズな操作感を実現しました。
* **サウンド抽出の高速化**: FSB5 (Ogg Vorbis) 音声抽出処理を 32-bit アライメントスキャンにより最適化し、従来比約4倍の超高速解析処理を実現しました。
* **ZIP生成・圧縮の最適化**: PNGやOGGなどの既圧縮メディアファイルに対し、無駄な再圧縮を回避する圧縮オプション選択を導入し、変換完了およびダウンロード生成時間を短縮しました。
* **UX・レスポンシブ性の改善**: メインスレッド処理時のイベントループ譲渡タイミングをミリ秒精度 (`performance.now()`) に最適化し、進捗UIの更新頻度と変換速度のバランスを向上させました。

## v1.3.0
* **機能追加**: 同一エディション内でのバージョン更新やダウングレード（Java ⇄ Java、Bedrock ⇄ Bedrock）をサポートする「バージョン変換」機能を追加しました。リソースパックのポルティング（Porting）用途にも最適です。
* **UI改善**: 変換方向の選択UIを整理し、「Java → Bedrock」「Bedrock → Java」「バージョン変換」の3つのボタンから直感的に選べるように刷新しました。
* **UI改善**: タブ切り替え時の高さ調整アニメーションを、残像や引き伸ばしなく滑らかに動くように最適化しました。
* **UX改善**: 「バージョン変換」選択時に、アップロードされたファイルの中身（`pack.mcmeta` / `manifest.json`）から元のエディションを自動判別し、UIを最適化する機能を追加しました。
* **バグ修正**: 同一エディション間の変換（Bedrock → Bedrock）時に出力ファイルが `.zip` になってしまう問題を修正し、正しく `.mcpack` で出力されるようにしました。
* **バグ修正**: 「GUI変換を有効にする」のオプション設定が、同一エディション間のバージョン更新時にも誤って適用され、GUI画像がスキップされてしまう問題を修正しました。
* **SEO・多言語対応**: トップページの「主な特徴」にバージョン変換に関する説明を追加し、検索流入を強化しました。また、「バージョンアップデート」という表現をダウングレード時にも違和感のない「バージョン変更（Change Versionなど）」へと各言語（日・英・韓・中・西・仏・独）で修正・最適化しました。

## v1.2.0
* **機能追加**: Minecraft 26系（Java版および統合版）の変換サポートを追加しました。
* **UI改善**: バージョン選択の表記を整理し、最新のマイナーバージョンを含む形で「26.x」として統一しました。これにより、より直感的にバージョンを選択できるようになりました。
* **バグ修正**: サウンドマッピングのタイポ修正、メモリリークの解消、サウンド変換の出力パス修正など、複数の内部バグを修正しました。
* **内部改善**: ブロック・アイテムのテクスチャ生成がバージョン別マッピングを正しく参照するよう改善しました。

## v1.1.0 (および初期リリース)
* **コア機能**: Minecraft Java版と統合版（Bedrock版）間のリソースパック相互変換エンジンを実装しました。
* **テクスチャ変換**: ブロック、アイテム、エンティティなどのテクスチャパスの自動マッピングおよび正規化に対応。
* **テキスト・サウンド**: 言語ファイル（`.lang` ⇔ `.json`）、サウンド定義ファイルの相互変換をサポート。
* **UI/UX**: ドラッグ＆ドロップで簡単に利用できるコンバーターUIを実装。
* **多言語対応**: UIを9言語（日本語、英語など）に対応させ、幅広いユーザーが利用できるようにしました。
* **その他**: 基本的なレイアウトの構築、SEO対策、サイトマップの生成機能を追加しました。
